%h1 All Posts

%table
  %tr
    %th Title
    %th Text
    %th
  - @posts.each do |post|
    %tr
      %td
        = post.title
      %td
        = post.text
      %td
        = link_to 'Show', post_path(post)

= link_to 'Home', root_url
